<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Recipes</title>
  
  <style>
    body {
      font-family: Arial, sans-serif;
      background-color: #f4f4f4;
      margin: 0;
      padding: 0;
      position: relative; /* For absolute positioning within body */
    }

    .recipe-container {
      display: grid;
      grid-template-columns: repeat(auto-fill, minmax(200px, 1fr));
      gap: 20px;
      padding: 20px;
    }

    .recipe-card {
      background-color: white;
      border: 1px solid #ddd;
      border-radius: 8px;
      padding: 16px;
      text-align: center;
      box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
    }

    .recipe-card img {
      width: 100%;
      height: 150px;
      object-fit: cover;
      border-bottom: 1px solid #ddd;
    }

    .recipe-card h2 {
      margin: 16px 0;
    }

    .recipe-card a {
      color: #007bff;
      text-decoration: none;
    }

    .home-button {
      position: absolute;
      top: 20px;
      left: 20px;
      padding: 10px 20px;
      text-align: center;
      background-color: #007bff;
      color: white;
      text-decoration: none;
      border-radius: 5px;
      font-weight: bold;
      font-size: 16px;
    }

    .home-button:hover {
      background-color: #0056b3;
    }
  </style>
</head>
<body>
  <a href="/" class="home-button">Home</a> <!-- Moved to the top-left corner -->

  <h1 style="text-align: center; margin: 60px 0 20px 0;">All Recipes</h1> <!-- Adjusted top margin to make space for the button -->
  
  <div class="recipe-container">
    <% recipes.forEach(recipe => { %>
      <div class="recipe-card">
        <img src="<%= recipe.image %>" alt="<%= recipe.name %>">
        <h2><%= recipe.name %></h2>
        <p><%= recipe.description %></p>
      </div>
    <% }); %>
  </div>
  <p style="text-align: center; font-size:2rem; color: #555; padding: 20px 0;">
    Made with <span style="color: red;">&#10084;&#65039;</span> by Ujjwal
</p>
</body>
</html>
